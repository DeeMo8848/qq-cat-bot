# 机器人运行时依赖（主进程 + 解析）。meme 子进程所需的 meme-generator 详见 install.ps1（固定 0.1.14）。
qq-botpy==1.2.1
aiohttp
aiofiles
msgspec
tqdm
Pillow
pil_utils
yt-dlp
httpx
curl_cffi
beautifulsoup4
apscheduler
bilibili-api
apilmoji
# Webhook 回调地址验证（op=13）要用 ed25519 签名；缺它会导致腾讯侧验证 500、
# 进而完全不推送消息（现象：bot 在线但 @ 和不@都没反应）。见 bot/core/webhook.py。
cryptography