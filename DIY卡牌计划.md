# 🃏 DIY 卡牌 计划

> 状态：**已实装**（代码与配置就绪，待重启 bot 验证；见文末「实装记录」）
> 优先级：已接入 qqbot

## 〇、最新计划（2026-09-15）

- 卡牌制作工具：**cardforge**（`D:\略夹\ai\ai工具\cardforge`，GitHub: `DeeMo8848/cardforge`）
- 新部署 bot 时通过 install.ps1 拉取该仓库（类似 meme 聚合仓库），bot 用命令调用 `cardforge.cmd`
- 制作产物在 `assets/output/<slug>/`（`card.png` 2D 合成图 + `cards/<slug>.json` 定义）
- 允许修改 cardforge 以接入 bot，但**不破坏工具原有功能**
- 卡牌网页化方向保留（预览链接 / 收集册 / 市场均为网页链接）

## 〇.1 已确认决策（2026-09-15）

| 项 | 决策 |
|---|---|
| 品级档位 | **铜 / 银 / 金 / 虹** 四档 |
| 价格区间 | 铜 100-300 / 银 500-1200 / 金 2000-5000 / 虹 8000-20000 喵币；**AI 只判品级，价格按档位区间内随机** |
| AI 视觉 | DeepSeek 官方视觉模型 `deepseek-v4-flash-vision-exp`（已支持识图）；品级判定传图像给该模型 |
| AI 提示词 | 由开发侧准备（见「提示词」节） |
| 制作费用 | **收制作费**（每次固定费用，形成消费闭环 + 防刷） |
| 销毁卡牌 | **无补偿**，直接删除 |
| 素材包 | **购买后永久使用**（原消耗品方案作废） |
| 链接方案 | **动态路由**（一个网页模板，URL 带不可猜 userKey，如 `/album/ab12cd34`；服务端按用户数据渲染，数据实时更新无需重生成文件） |
| 制作队列 | 缓存区串行队列，前一个制作完成后才进行下一个 |
| 卡牌特效 | bot 命令中占位，不实装不展示 |
| AI 上下文 | 当前 ai_config 已配 DeepSeek key，品级判定独立调用（不受 AI 对话 enabled 开关影响） |

## 一、卡牌组成元素

| 元素 | 必选/可选 | 说明 |
|---|---|---|
| 卡牌卡面 | **必选** | 制作时用户**引用的图像** |
| 卡牌名称 | **必选** | 卡牌 id/名称 |
| 卡牌边框 | 可选 | 不填用默认 |
| 卡牌背景 | 可选 | 不填用默认 |
| 卡牌牌背 | 可选 | 不填用默认 |
| 卡牌卡封 | 可选 | 不填用默认 |
| 卡牌特效 | 可选 | **暂不实装进 bot，命令里占位，不起实际作用** |
| 边框特效 | 可选 | 不填用默认 |
| 卡牌品级（稀有度） | 由 AI 判定 | 铜 / 银 / 金 / 虹（见决策表） |
| 卡牌描述 | 由 AI 判定 | 文本型/特殊文本型时提供 |
| 卡牌标题 | 由 AI 判定 | 文本型/特殊文本型时提供 |

## 二、制作规则

- **必选参数**：卡牌卡面（引用图像）+ 卡牌名称
- **可选参数**：边框、背景、牌背、卡封、特效（占位）、边框特效——未填写时工具自动选用默认值
- **额外可选参数**（README 有写明）：主体浮于边框之上、主体描边、裁剪到边框内、卡牌类型、自适应裁剪、缩放、整幅/透明主体、是否抠图

### 特殊规则

- 使用**整幅卡面**时：忽略「主体浮于边框之上」和「主体描边」两个参数
- 使用**整幅卡面 + 抠图**时：忽略「背景」

## 三、触发命令示例

| 命令 | 结果 |
|---|---|
| `卡牌制作 xxx` | ❌ 制作失败（缺少图像） |
| `"引用的图像" 卡牌制作` | ❌ 制作失败（缺少参数，至少起个名） |
| `"引用的图像" 卡牌制作 xxx` | ✅ 基础制作 |
| `"引用的图像" 卡牌制作 xxx 边框xx 背景xx` | ✅ 指定边框+背景 |
| `"引用的图像" 卡牌制作 xxx 边框xx 背景xx 牌背xx 卡封xx 卡牌特效xx 边框特效xx` | ✅ 全指定（卡牌特效仅占位） |
| `"引用的图像" 卡牌制作 xxx 边框精灵 背景星空 牌背 卡封 边框特效` | ✅ 关键词未给值用默认 |
| `"引用的图像" 卡牌制作 xxx 边框精灵 背景星空 牌背 主体悬浮 描边` | ✅ 出现关键词=启用开关 |
| `"引用的图像" 卡牌制作 xxx 边框精灵 背景星空 卡封十字 缩放0.8 整幅 不抠图 卡牌类型特殊文本` | ✅ 组合参数（默认无文本） |

## 四、bot 卡牌制作流程

1. 用户引用图片 + 触发命令
2. **图像 + 提示词 交给 AI（视觉模型）**，让 AI 提供：
   - **卡牌品级**（铜 / 银 / 金 / 虹）
   - 若制作类型为**文本型或特殊文本型**：额外让 AI 提供**卡牌描述**和**卡牌标题**
   - 价格不劳烦 AI：按品级档位区间随机（铜 100-300 / 银 500-1200 / 金 2000-5000 / 虹 8000-20000）
3. AI 返回 JSON 结果（`rarity` + 可选 `title`/`desc`）**参与卡牌制作**（不通过 bot 文本发送）
4. 扣除制作费 → 参数集齐后调用 **cardforge** 制作卡牌（串行队列）
5. 制作完成后返回**单张卡牌预览链接**给用户
6. 后台将卡牌加入**用户对应的收集册**

### AI 品级判定提示词（2026-09-15 定稿）

> 已确认：`deepseek-v4-flash-vision-exp` 是 DeepSeek 官方多模态模型（2026-08-21 上线，
> 支持 Chat Completions 传图，单图最高 384 token），模型 ID 无误。bot 实装于
> `plugins/cards/ai_judge.py`，base_url/api_key 复用 `cache/ai_config.json`，
> 模型名取该文件的 `vision_model` 字段（默认 `deepseek-v4-flash-vision-exp`）。

**给 AI 的完整提示词（系统提示，bot 内置）：**

```
你是「卡牌品级判定师」，只根据用户提供的卡面图像做客观判定，不闲聊、不解释。
判定标准（严格按图像的内容质量与完成度，与题材、画风无关）：
- 铜：普通日常照 / 随手拍 / 简单涂鸦 / 低清模糊 / 截图 / 完成度低的素材
- 银：较精美 / 主体清晰 / 有明确主题和构图的插画或照片 / 完成度中等
- 金：精美高质量 / 精致立绘 / 光影构图讲究 / 高完成度 / 适合收藏
- 虹：顶级 / 大师级 / 绝美 / 神级 / 极具艺术与收藏价值的作品
规则：
1. 只输出一个 JSON 对象，禁止输出任何其他文字、解释或 Markdown 代码块围栏。
2. JSON 结构固定为：{"rarity":"铜|银|金|虹","title":"","desc":""}
3. rarity 只能取「铜 / 银 / 金 / 虹」之一。
4. 若本次为文本型卡牌（用户要求带标题和描述）：title 给一个不超过 8 字、贴合画面的卡牌标题；
   desc 给一句不超过 30 字、有游戏卡牌感的「效果描述」（可虚构，类似「登场时：使我方全体攻击力+2」）。
   若为非文本型卡牌，title 和 desc 都必须为空字符串 ""。
5. 判定宁严勿松：普通质量的图片不要轻易给金 / 虹。
```

**调用细节：**
- 用户消息 = 图像（OpenAI 兼容 `image_url`，base64 data URL）+ 一句附加说明
  （文本型：「本次为文本型卡牌，请按规则给出标题与效果描述」/ 无文本型：「本次为无文本型卡牌，仅判定品级」）
- temperature 用 0.2（判定任务要稳定），超时 90 秒
- 解析兜底：输出非法 / 请求失败 → 品级降为「铜」、title/desc 置空，不中断制作流程
- 用户手动给了「标题/描述」参数时优先用用户的，AI 只补缺项；两者都有则 AI 只判品级

## 五、bot 可用命令概览

| 命令 | 说明 |
|---|---|
| `制作卡片` | 列出功能概览 |
| `"引用的图像" 卡牌制作` | 制作卡牌（触发命令，见上） |
| `收集册` | 返回用户对应的收集册链接 |
| `我的卡牌` | 文本形式列出用户持有的卡牌名称和价值 |
| `销毁卡牌 xxx` | 删除用户收集册中指定的卡牌 |
| `卡牌市场` | 新增玩法：卡牌可挂出售和购买（类似钓鱼插件的鱼市）。返回**网页链接**查看市场，但**购买/出售仍通过 QQ 聊天文本** |
| `上架卡牌 xxx 800` | 把用户指定卡牌从收集册移动到卡牌市场。出售后**不会立即获得喵币**（交易方式参考钓鱼插件） |
| `购买卡牌 xxx` | 把指定卡牌从市场移动到购买者收集册，扣除对应喵币，卖家获得一定喵币 |

## 六、喵币经济

- **素材包改为「购买后永久使用」**（2026-09-15 修订，原"消耗品"方案作废）：
  - 喵币购买各种卡牌外观（边框/背景/牌背/卡封等素材包）
  - 购买后永久持有，不消耗
- 卡牌制作费用 + 市场交易形成消费与获得闭环（具体定价/税率待定，参考钓鱼市场）

## 七、网页化方向（定稿：动态路由）

**卡牌用网页呈现，bot 发公网链接。** 静态页面服务已就绪（`bot/public_html/` + `page.deemo8848.dpdns.org`，2026-09-05 已实测：电脑无提示、手机 QQ 仅"谨慎访问"软提示不拦截）。

**链接方案已定（2026-09-15）：动态路由** —— 一个 HTML 模板，URL 带不可猜的 userKey：

- 单卡预览：`https://page.deemo8848.dpdns.org/card/<cardKey>`（cardKey = 卡牌 ID 哈希，不可猜）
- 收集册：`https://page.deemo8848.dpdns.org/album/<userKey>`（userKey = HMAC/SHA256(openid, secret) 截断，不可猜防遍历）
- 卡牌市场：`https://page.deemo8848.dpdns.org/market`
- 数据实时读取 `data/cards/` 渲染，卡牌变动刷新即变，无需重生成文件
- 收集册**本身可公开分享**（用户明确允许他人查看），无需登录/token
- 动态路由在 `bot/core/static_server.py` 中扩展，白名单路由 + `public_html/` 之外仍 404，安全模型不变

路由清单：
| 路由 | 说明 |
|---|---|
| `/card/<cardKey>` | 单张卡牌预览页（3D/静态卡面展示，可翻面看效果） |
| `/album/<userKey>` | 用户收集册（卡片网格，点开可看单卡） |
| `/market` | 卡牌市场（展示挂单中的卡牌；购买/出售仍走 QQ 文本） |

## 八、待定事项

- [x] **卡牌链接实现方式** → 已定：动态路由
- [x] **品级档位与价格** → 已定：铜银金虹 + 区间随机
- [x] **AI 视觉模型** → 已定：deepseek-v4-flash-vision-exp
- [x] **AI 提示词** → 已定稿（见「四、AI 品级判定提示词」节）
- [x] **制作费金额** → 200 喵币/次（settings.json `CARD_MAKE_COST` 可改）
- [x] **销毁补偿** → 无补偿，直接删除（含取消市场挂单）
- [x] **制作队列** → 缓存区串行队列（asyncio 单 worker，前一个完成才做下一个）
- [x] **卡牌特效** → bot 命令占位，不实装不展示
- [x] **市场上架/交易** → 参考钓鱼市场：买家付全款、卖家即时到账 90%、10% 交易税回收
- [x] **素材包具体品类与定价** → 初版已定：背景 500 / 边框 800 / 卡封 1000 / 牌背 600 / 边框特效 800 喵币（settings.json `CARD_MATERIAL_PRICES` 可改）；素材清单自动扫描 cardforge assets/ 目录（背景 4 / 卡封 34 / 辉光内置 11 种；边框与牌背目前各 1 种，可自行往 assets 目录加素材扩充）
- [x] **实装联调** → 已通过：cardforge 冒烟测试（真实出卡）、数据层 32 项测试、静态路由 11 项测试、模块导入检查

## 九、实装记录（2026-09-15）

- 代码落地于 `plugins/cards/`：`commands.py`（11 条命令）、`carddata.py`（数据层）、`forge.py`（cardforge 调用 + 串行队列）、`ai_judge.py`（AI 品级判定）、`web/`（card/album/market 三套网页模板）
- 动态路由已接入 `bot/core/static_server.py`（/card /album /market /assets /api/*），webui 已加「卡牌制作」模块总开关
- `install.ps1` 增加 cardforge 克隆步骤（tools/cardforge）；`settings.json` 已配 `CARD_DIR` 指向现有工具目录
- 品级判定用 `cache/ai_config.json` 的 `vision_model`（默认 deepseek-v4-flash-vision-exp，缺省回退），base_url/api_key 复用，不受 AI 对话开关影响
- 素材商店：`/market` 网页加 6 个分页（市场/背景/边框/卡封/牌背/辉光），素材带图像预览；购买走 QQ 命令「购买背景 名称」「购买辉光 名称」等快捷格式（兼容「购买素材 类型 名称」）
- 待验证：重启 bot（先停掉再双击 `启动bot.bat`）→ 群里发「卡牌制作 卡名」引用图片全流程走通
